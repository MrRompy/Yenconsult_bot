services:
  - type: web
    name: yenkonsult_bot
    env: python
    buildCommand: |
      pip install --upgrade pip setuptools wheel
      pip install -r requirements.txt
    startCommand: python bot.py
    autoDeploy: true

